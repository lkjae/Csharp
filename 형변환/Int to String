-Convert.ToString()
int num = 30;
string numStr = Convert.ToString(num);

-String.Format()
int num = 30;
string numStr = string.Format("{0}", num);
//{0}은 형식 항목이고 0은 해당 위치에 문자열 표현이 삽입 된 객체의 시작 색인

-StringBuilder
using System.Text;
int num = 30;
string numStr = new StringBuilder().Append(num).ToString();
